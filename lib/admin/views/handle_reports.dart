import 'package:flutter/material.dart';

class HandleReports extends StatelessWidget {
  const HandleReports({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}